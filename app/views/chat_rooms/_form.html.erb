<%= turbo_frame_tag :chat_room_form do %>
  <%= form_for new_chat_room do |f| %>
    <%= f.fields_for :chat_room_users do |ff| %>
      <%= ff.text_field :user_id, class: ff.object.user_id == user.id ? 'hidden' : '' %>
    <% end %>
    <%= f.submit %>
  <% end %>
<% end %>